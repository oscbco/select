export function isKeyDown (keycode) {
  return keycode === 40;
}

export function isKeyUp (keycode) {
  return keycode === 38;
}

export function isKeyEnter (keycode) {
  return keycode === 13;
}

export function isKeyEsc (keycode) {
  return keycode === 27;
}
